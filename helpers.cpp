/*int sumarSegundos() {
    int suma = 0;
    for (int i = 0; i < numArreglos; ++i) {
        // Verificar que haya al menos dos elementos en el subarreglo
        if (arregloDelineametro[i][1] != -1) {
            suma += arregloDelineametro[i][1]; // Sumar el segundo elemento del subarreglo
        }
    }
    return suma;
}
*/
